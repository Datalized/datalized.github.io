{
  "name": "paes2026",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "PATH=.venv/bin:$PATH observable preview",
    "build": "PATH=.venv/bin:$PATH observable build",
    "clean": "rm -rf dist .observablehq/cache"
  },
  "dependencies": {
    "@observablehq/framework": "^1.12.0"
  },
  "devDependencies": {}
}
